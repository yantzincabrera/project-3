const expect = require('chai').expect;
const User = require('./User.controller');

describe('Controller - User', () => {
  beforeEach(() => {
    // TODO - Test Pre-Conditions Here
  });

  describe('create', () => {
    it('should create a single User', () => {
      expect(true).to.equal(false);
    });
  });

  describe('getById', () => {
    it('should return a single User with the matching id', () => {
      expect(true).to.equal(false);
    });
  });

  describe('getAll', () => {
    it('should return an array of all User', () => {
      expect(true).to.equal(false);
    });
  });

  describe('updateById', () => {
    it('should update a single User with the matching id', () => {
      expect(true).to.equal(false);
    });
  });

  describe('deleteById', () => {
    it('should delete a single User with the matching id', () => {
      expect(true).to.equal(false);
    });
  });
});
