const expect = require('chai').expect;
const Blog = require('./Blog.controller');

describe('Controller - Blog', () => {
  beforeEach(() => {
    // TODO - Test Pre-Conditions Here
  });

  describe('create', () => {
    it('should create a single Blog', () => {
      expect(true).to.equal(false);
    });
  });

  describe('getById', () => {
    it('should return a single Blog with the matching id', () => {
      expect(true).to.equal(false);
    });
  });

  describe('getAll', () => {
    it('should return an array of all Blog', () => {
      expect(true).to.equal(false);
    });
  });

  describe('updateById', () => {
    it('should update a single Blog with the matching id', () => {
      expect(true).to.equal(false);
    });
  });

  describe('deleteById', () => {
    it('should delete a single Blog with the matching id', () => {
      expect(true).to.equal(false);
    });
  });
});
